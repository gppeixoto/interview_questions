public class c1p4
{
    public static boolean repl(char[] s, int len) {
        // replace every space with %20
        // assume string has enough space in the end
        int curr;
        for (int i=0; i < len; ) {
            curr = i;
            
        }
    }
}